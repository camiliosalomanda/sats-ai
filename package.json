{
  "name": "sats-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized AI inference marketplace on Bitcoin + Nostr",
  "workspaces": [
    "apps/client",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/client",
    "build": "npm run build -w packages/shared && npm run build -w apps/client",
    "lint": "npm run lint -w apps/client"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "nostr-tools": "^2.23.3"
  }
}
